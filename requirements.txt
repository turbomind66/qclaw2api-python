# qclaw2api runtime deps (only two; everything else is stdlib)
requests>=2.31.0

# optional: only needed when upstash.url / upstash.token are configured
# (without it the pool state auto-degrades to in-memory mode)
redis>=5.0.0
